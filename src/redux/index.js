import { mainAction, mainReducer, mainSelect } from './main';
import { userAction, userReducer, userSelect } from './user';

export { mainAction, mainReducer, mainSelect, userAction, userReducer, userSelect };
