import { mainAction, mainReducer, mainSelect } from './main';
import { userAction, userReducer, userSelect } from './user';
import { tokenAction, tokenReducer, tokenSelect } from './token';
import { roleAction, roleReducer, roleSelect } from './role';
import { permissionAction, permissionReducer, permissionSelect } from './permission';
import { dirAction, dirReducer, dirSelect } from './dir';

export {
  mainAction,
  mainReducer,
  mainSelect,
  userAction,
  userReducer,
  userSelect,
  tokenAction,
  tokenReducer,
  tokenSelect,
  roleAction,
  roleReducer,
  roleSelect,
  dirAction,
  dirReducer,
  dirSelect,
  permissionAction,
  permissionReducer,
  permissionSelect
};
